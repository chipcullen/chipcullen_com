---json
{
  "permalink": "index.xml",
  "eleventyExcludeFromCollections": true,
  "metadata": {
    "title": "Chip Cullen",
    "subtitle": "Mission driven web developer",
    "description": "Recent content on ChipCullen.com. My articles are about front end development, and development in general.",
    "language": "en",
    "url": "https://chipcullen.com/",
    "author": {
      "name": "Chip Cullen",
      "email": "chip@chipcullen.com"
    }
  }
}
---
<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xml:base="{{ metadata.url }}" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ metadata.title }}</title>
    <link>{{ metadata.url }}</link>
    <atom:link href="{{ permalink | absoluteUrl(metadata.url) }}" rel="self" type="application/rss+xml" />
    <description>{{ metadata.subtitle }}</description>
    <language>{{ metadata.language }}</language>
    {%- for post in collections.posts | reverse %}
    {%- set absolutePostUrl = post.url | absoluteUrl(metadata.url) %}
    <item>
      <title>{{ post.data.title }}</title>
      <link>{{ absolutePostUrl }}</link>
      {% if post.data.description %}
      <description>{{ post.data.description }}</description>
      {% endif %}
      <pubDate>{{ post.date | dateToRfc822 }}</pubDate>
      <guid>{{ absolutePostUrl }}</guid>
    </item>
    {%- endfor %}
  </channel>
</rss>
